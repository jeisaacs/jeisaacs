# Managed by Terraform (github-tf). Do not edit directly — change the
# github_repository_file resource in security-scanning.tf instead.
name: Security Scanning

on:
  pull_request:
  push:
    branches: ["main"]

permissions:
  contents: read

jobs:
  security:
    uses: jeisaacs/jeisaacs/.github/workflows/reusable-security.yml@main
    with:
      python: false
